package pt.raizesdigitais.app.ui.theme

import androidx.compose.ui.graphics.Color

// Cores da Quinta da Azenha — Style Guide
// Paleta que nasce do Arinto no copo: ouro pálido, luz atlântica, mineral branco

// Cores principais
val VerdeMusgo = Color(0xFF4A7C2F)        // Primária — folha, vinha, exterior
val VerdeMusgoEscuro = Color(0xFF3A6022)  // Hover/pressed
val OuroJovem = Color(0xFFE2E5AC)         // Secundária — Arinto no copo
val BrancoMineral = Color(0xFFFDFDFB)     // Fundo — pedra calcária
val VerdeEscuro = Color(0xFF2F3E1E)       // Texto principal
val VerdeTextoLeve = Color(0xFF6B7A5C)    // Texto secundário, legendas

// Cores de acção
val Dourado = Color(0xFFC9A84C)           // Destaque — preços, CTAs premium
val Sucesso = Color(0xFF27AE60)           // Confirmação
val Alerta = Color(0xFFC0392B)            // Erros, stock baixo
val Neutro = Color(0xFFE8E8E4)           // Bordas, divisores
val NeutroClaro = Color(0xFFF5F5F3)       // Cards, backgrounds secundários